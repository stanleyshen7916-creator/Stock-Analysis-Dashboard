import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { chromium } from 'playwright';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
const pages=['dashboard','portfolio','analysis','engine','strategy','market','industry','backtest','data','reports','settings','top50'];
const server=http.createServer((req,res)=>{const p=path.join(root,req.url.split('?')[0]==='/'?'/index.html':req.url.split('?')[0]);fs.readFile(p,(e,d)=>{res.writeHead(e?404:200,{'Content-Type':p.endsWith('.js')?'text/javascript':'text/html'});res.end(e?'not found':d);});});
const text=async(p,sel)=>{if(await p.locator(sel).count()===0)return '';return(await p.locator(sel).first().textContent())||''};
const loading=v=>String(v).includes('載入中');
async function check(page,label,problems){const hero=await text(page,'#hero-count'),table=await text(page,'#stock-table');if(!hero)problems.push(`${label}: dashboard hero-count missing/blank`);if(loading(hero)||loading(table))problems.push(`${label}: dashboard stuck loading`);if(!(await page.locator('#stock-table .link-stock').count())&&!table.includes('Production Data 讀取失敗')&&!table.includes('尚無'))problems.push(`${label}: dashboard Top 10 has no real row/state`);const stock=page.locator('.link-stock').first();if(await stock.count())await stock.click();else{await page.locator('[data-page="analysis"]').first().click();const inp=page.locator('#analysis-symbol');if(await inp.count()){await inp.fill('2330');await page.locator('#analysis-search').click()}}await page.waitForTimeout(1400);for(const id of ['#analysis-conclusion','#analysis-provenance','#analysis-fundamentals','#analysis-market']){const v=await text(page,id);if(!v)problems.push(`${label}: ${id} blank`);if(loading(v))problems.push(`${label}: ${id} stuck loading`)}const tabs=page.locator('.stock-tab');if(await tabs.count()!==11)problems.push(`${label}: stock tab count != 11`);for(let i=0;i<await tabs.count();i++){await tabs.nth(i).click();await page.waitForTimeout(150);const v=await text(page,'#analysis-tab-content');if(!v)problems.push(`${label}: stock tab ${i+1} blank`);if(loading(v))problems.push(`${label}: stock tab ${i+1} loading`)}}
async function matrix(page,label,problems){for(const name of pages){const b=page.locator(`[data-page="${name}"]`).first();if(await b.count()===0){problems.push(`${label}: navigation page ${name} missing`);continue}await b.click();await page.waitForTimeout(name==='top50'?2400:1200);const rootNode=page.locator(`#page-${name}`).first(),v=await text(page,`#page-${name}`);if(await rootNode.count()===0)problems.push(`${label}: page ${name} root missing`);else if(!v.trim())problems.push(`${label}: page ${name} blank`);if(loading(v))problems.push(`${label}: page ${name} stuck loading`);if(name==='top50'){const t=await text(page,'#top50-table');if(!(await page.locator('#top50-table tr').count()))problems.push(`${label}: top50 table empty`);if(!t.includes('2330')&&!t.includes('Production Data 讀取失敗')&&!t.includes('尚無'))problems.push(`${label}: top50 has no verified row/state`)}}}
async function run(){await new Promise(r=>server.listen(0,'127.0.0.1',r));const port=server.address().port,browser=await chromium.launch();let failed=false;for(const[label,viewport]of[['Desktop',{width:1280,height:900}],['Mobile',{width:390,height:844}]]){const p=await browser.newPage({viewport}),errors=[];p.on('console',m=>{if(m.type()==='error'&&!m.text().includes('favicon'))errors.push(m.text())});p.on('pageerror',e=>errors.push(e.message));await p.goto(`http://127.0.0.1:${port}/index.html`,{waitUntil:'domcontentloaded',timeout:15000});await p.waitForTimeout(3200);const problems=[];await check(p,label,problems);await matrix(p,label,problems);const overflow=await p.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth+1);console.log(`${label}: errors=${errors.length} overflow=${overflow} problems=${problems.length}`);[...errors,...problems].forEach(x=>console.log(' - '+x));if(errors.length||overflow||problems.length)failed=true;await p.close()}await browser.close();server.close();if(failed)process.exit(1);console.log('QA PASSED')}run().catch(e=>{console.error(e);process.exit(1)});
