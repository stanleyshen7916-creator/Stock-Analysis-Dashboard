name: One-time Dashboard Font Patch

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[font-patch]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch reference dashboard typography
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          marker = '/* Typography visual minimum v3 */'
          if marker not in s:
              patch = '''\n    /* Typography visual minimum v3 */\n    body{zoom:1.333333!important}\n    @media(max-width:800px){body{zoom:1.333333!important}}\n'''
              s = s.replace('</style>', patch + '  </style>', 1)
              p.write_text(s, encoding='utf-8')
          PY
      - name: Commit patch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'fix: enforce 12px minimum dashboard typography [font-patch]'
          git push
      - name: Remove temporary workflow
        run: |
          git rm .github/workflows/one-time-font-patch.yml
          git commit -m 'chore: remove one-time typography patch runner [font-patch]'
          git push
